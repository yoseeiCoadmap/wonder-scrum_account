import React, { FC, Fragment } from "react";
import TopRouter from "./presentation/TopRouter";

// Configurator
const TopScene: FC = () => {
  return (
    <Fragment>
      <TopRouter />
    </Fragment>
  );
};

export default TopScene;
